#include "ContentPath.hpp"

ContentPath::ContentPath()
{
    // std::string currentDir = get_current_dir_name();
    // path = currentDir + "/content";

    path = "content/";

    // char* path = get_current_dir_name();
    // free(path);
}